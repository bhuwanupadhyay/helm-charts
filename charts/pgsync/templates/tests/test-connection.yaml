apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pgsync.fullname" . }}-test-connection"
  labels:
    {{- include "pgsync.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pgsync.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
