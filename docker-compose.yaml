version: "3.0"
services:
  ecr-creds:
    image: docker.io/zbytes/ecr-creds:1.0.0
    build:
      context: docker/ecr-creds
      dockerfile: Dockerfile
  pgsync:
    image: docker.io/zbytes/pgsync:1.0.0
    build:
      context: docker/pgsync
      dockerfile: Dockerfile
